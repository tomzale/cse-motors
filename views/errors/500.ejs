<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - CSE Motors</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" media="screen">
    <style>
        .error-container {
            min-height: 70vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            background: linear-gradient(135deg, #fff5f5 0%, #ffe6e6 100%);
        }
        
        .error-content {
            text-align: center;
            max-width: 600px;
            padding: 3rem;
            background: white;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(230, 57, 70, 0.1);
            border: 2px solid #ffe6e6;
        }
        
        .error-code {
            font-size: 8rem;
            font-weight: 800;
            color: #e63946;
            line-height: 1;
            margin: 0;
        }
        
        .error-title {
            font-size: 2.5rem;
            color: #212529;
            margin: 1rem 0;
        }
        
        .error-message {
            font-size: 1.2rem;
            color: #6c757d;
            margin: 1.5rem 0 2.5rem;
            line-height: 1.6;
        }
        
        .error-actions {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        .error-btn {
            display: inline-block;
            padding: 0.75rem 2rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }
        
        .error-btn-primary {
            background: #e63946;
            color: white;
        }
        
        .error-btn-primary:hover {
            background: #c1121f;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(230, 57, 70, 0.2);
        }
        
        .error-btn-secondary {
            background: transparent;
            color: #e63946;
            border-color: #e63946;
        }
        
        .error-btn-secondary:hover {
            background: #e63946;
            color: white;
            transform: translateY(-2px);
        }
        
        .error-icon {
            font-size: 4rem;
            margin-bottom: 1.5rem;
            color: #e63946;
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        .debug-info {
            margin-top: 2rem;
            padding: 1rem;
            background: #f8f9fa;
            border-radius: 8px;
            border-left: 4px solid #e63946;
            text-align: left;
        }
        
        .debug-info h4 {
            margin-top: 0;
            color: #e63946;
        }
        
        @media (max-width: 768px) {
            .error-code {
                font-size: 6rem;
            }
            
            .error-title {
                font-size: 2rem;
            }
            
            .error-content {
                padding: 2rem;
                margin: 1rem;
            }
            
            .error-actions {
                flex-direction: column;
            }
            
            .error-btn {
                width: 100%;
                text-align: center;
            }
        }
        
        @media (max-width: 480px) {
            .error-code {
                font-size: 4rem;
            }
            
            .error-title {
                font-size: 1.5rem;
            }
            
            .error-content {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    <%- include('../partials/nav') %>
    
    <main class="error-container">
        <div class="error-content">
            <div class="error-icon">⚡</div>
            <h1 class="error-code">500</h1>
            <h2 class="error-title"><%= title %></h2>
            <p class="error-message"><%= message %></p>
            <p class="error-message">This error was triggered by the "Test Error Handling" link in the footer (for CSE 340 assignment testing).</p>
            
            <div class="error-actions">
                <a href="/" class="error-btn error-btn-primary">
                    ← Return to Home
                </a>
                <a href="mailto:support@csemotors.com" class="error-btn error-btn-secondary">
                    Contact Support
                </a>
                <a href="javascript:location.reload()" class="error-btn error-btn-secondary">
                    ↻ Refresh Page
                </a>
            </div>
            
            <% if (process.env.NODE_ENV !== 'production') { %>
            <div class="debug-info">
                <h4>Debug Information (Development Only):</h4>
                <p><strong>Error:</strong> Intentional server error triggered from footer link</p>
                <p><strong>Purpose:</strong> CSE 340 Assignment 3 - Error Handling Demonstration</p>
                <p><strong>Timestamp:</strong> <span id="timestamp"></span></p>
                <p><strong>Path:</strong> <%= typeof req !== 'undefined' ? req.originalUrl : 'N/A' %></p>
            </div>
            <% } %>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Set timestamp
            const now = new Date();
            document.getElementById('timestamp').textContent = now.toLocaleString();
            
            // Add button animations
            const buttons = document.querySelectorAll('.error-btn');
            buttons.forEach(button => {
                button.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-2px)';
                });
                button.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                });
            });
            
            // Animate the error code
            const errorCode = document.querySelector('.error-code');
            errorCode.style.opacity = '0';
            errorCode.style.transform = 'scale(0.5)';
            
            setTimeout(() => {
                errorCode.style.transition = 'all 0.8s cubic-bezier(0.34, 1.56, 0.64, 1)';
                errorCode.style.opacity = '1';
                errorCode.style.transform = 'scale(1)';
            }, 300);
        });
    </script>
</body>
</html>